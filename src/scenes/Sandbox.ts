import { SceneNames } from "Constants";
import PhaserScene from "Core/PhaserScene";

export default class Sandbox extends PhaserScene {
  constructor() {
    super(SceneNames.Sandbox);
  }

  create() {}
}
