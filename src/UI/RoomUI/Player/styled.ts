import styled from "styled-components";
import { BorderWrapper } from "UI";

export const PlayerWrapper = styled(BorderWrapper)`
  gap: 10px;
`;

export const PlayerButton = styled.button`
  width: 100%;
`;

export const KickButton = styled.button``;

export const PlayerGroup = styled.div`
  display: flex;
  gap: 8px;
`;
