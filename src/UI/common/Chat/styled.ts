import styled from "styled-components";
import { BorderWrapper } from "UI";

export const ChatWrapper = styled(BorderWrapper)`
  justify-content: flex-end;
`;

export const ChatInput = styled.input`
  width: 100%;
`;
